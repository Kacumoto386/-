# -*- coding: utf-8 -*-
"""V2.4.0 功能规划大纲"""
"""
============================================================
  gym-excel-system v2.4.0 功能规划
============================================================

## 方向三：数据看板增强（大屏/报表）

### 3.1 首页看板升级 (gui/dashboard_frame.py)
- 新增图表区域：使用 Canvas 绘制柱状图和趋势线
- 更多指标卡片：月度对比、教练排行、频次分布
- 大屏模式：全屏无边框展示，适合挂在墙上

### 3.2 新增报表看板 (gui/report_frame.py)
- 月报：本月经营概况 PDF/TXT 导出
- 教练业绩对比：售课额 + 上课数 + 提成排行
- 会员活跃度：到店频率分布
- 现金流曲线：每日收入趋势

### 3.3 新增：大屏模式
- 全屏/窗口切换
- 自动刷新（30秒轮询）
- 视觉优化：大字号、高对比度、深色模式

## 方向五：API接口层（为小程序/微信支付做准备）

### 5.1 API层 (api/ 目录)
- 统一 JSON 响应格式 {success, data, message}
- RESTful 风格：GET/POST 方法
- 数据序列化：Python dict → JSON
- 无外部依赖（仅用标准库 json）

### 5.2 接口清单
会员模块：
  - GET  /api/members          获取会员列表
  - GET  /api/members/:id      获取单个会员
  - POST /api/members          新增会员
  - PUT  /api/members/:id      更新会员
  - DELETE /api/members/:id    删除会员

课程模块：
  - GET  /api/courses           获取课程列表
  - GET  /api/courses/:id       获取单个课程

预约模块：
  - GET  /api/bookings          获取预约列表（支持日期筛选）
  - POST /api/bookings          创建预约（含冲突检查）
  - PUT  /api/bookings/:id/signin  签到

售课模块：
  - GET  /api/sales             获取售课列表
  - POST /api/sales             创建售课（含课程包联动）

### 5.3 支付预留
- 订单格式化输出（金额、状态、时间）
- 支付回调预留接口
- 退款处理预留

## 版本号更新
config.py: __version__ = "2.4.0"

## 文件结构变化
gym-excel-system/
├── api/                    # [新增] API接口层
│   ├── __init__.py
│   ├── server.py          # HTTP server (不用框架)
│   ├── serializers.py     # 数据序列化
│   └── routes.py          # 路由注册
├── gui/
│   ├── dashboard_frame.py # [修改] 首页看板升级
│   └── report_frame.py   # [新增] 报表看板
├── core/
│   └── business.py        # [修改] 增加统计类方法
└── config.py              # [修改] 版本号

"""


## 实际开发调整（2026-05-04）

### 确认范围
- ✅ **方向三：数据看板增强** — 已完成
- ✅ **方向五：API接口层** — 已完成（保留为日后小程序/支付体系对接）

### 调整说明
- API接口层已完成全功能开发（19个RESTful接口）
- 支付回调 `/api/payments/notify` 和退款 `/api/payments/refund` 保留为桩接口
- 后续需要时直接对接支付网关即可
- 小程序端目前不开发，API层作为基础设施保留
- API服务器命令：`python api/server.py --port 8765`
